# Optimized requirements for Render Cloud 2GB RAM deployment
# Core FastAPI with minimal dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Memory-optimized computer vision stack
opencv-python-headless==4.8.1.78  # Headless version saves ~50MB
Pillow==10.1.0

# TensorFlow with CPU optimizations
tensorflow-cpu==2.15.0  # CPU-only version saves ~200MB
numpy==1.24.3

# Essential API dependencies
pydantic==2.5.0
python-multipart==0.0.6

# Memory monitoring and optimization
psutil==5.9.6  # For memory monitoring
memory-profiler==0.61.0  # For memory profiling

# Production optimizations
gunicorn==21.2.0  # Production WSGI server
whitenoise==6.6.0  # Static file serving

# Additional optimizations for Render
httptools==0.6.0  # Fast HTTP parser
uvloop==0.19.0  # Fast event loop (if available)

# Optional: Additional optimizations
# Uncomment if needed for specific features
# aiofiles==23.2.1  # Async file operations
# redis==5.0.1  # For caching (if needed)
